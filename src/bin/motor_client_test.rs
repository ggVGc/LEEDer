// use std::{thread, time::Duration};
// use log::info;
// use leed_controller::motors_client::MotorsClient;

fn main() {
    env_logger::init();
    // let on_scan_start = || info!("Scan started!");
    // let mut motors = MotorsClient::new("/dev/ttyUSB0", on_scan_start).unwrap();
    // loop {
    //     motors.update(|_|{});
    //     let pos = motors.get_last_pos();
    //     println!("Pos: {:?}", pos);
    //     motors.set_pos(80, 60);
    //     thread::sleep(Duration::from_secs(1));
    // }
}
